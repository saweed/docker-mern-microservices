version: '3'
services:
  front_end:
    build: .
    ports:
      - "5000:3000"
    volumes:
      - ./src:/app/src
      - /app/node_modules
    environment:
      - REACT_APP_NAME=FRONT_END
      - CHOKIDAR_USEPOLLING=true
